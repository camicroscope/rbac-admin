/**
 * Compressor
 *
 * this function converts the JSON object from an expanded format
 * used in the application into a condenced format expected by the
 * server.
 */
export const compressor = (expanded) => {
  const compressed = {};

  return compressed;
};
